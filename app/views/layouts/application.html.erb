<!DOCTYPE html>
<html>
<head>
  <title>Dropify</title>
  <script src="https://maps.googleapis.com/maps/api/js?v=3.exp"></script>
  <script src="//cdnjs.cloudflare.com/ajax/libs/mustache.js/0.8.1/mustache.min.js"></script>
  <meta name="viewport" content="initial-scale=1, maximum-scale=1, user-scalable=no, width=device-width, minimal-ui">
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <%= yield %>

  <script id="messageTpl" type="text/template">
    <div class="messageTemplate">

      {{#picturePath}}
        <div class ="message-image">
          <img src={{picturePath}}>
        </div>
      {{/picturePath}}

        <p><span class="username">Posted by:  {{user.username}}</span></p>
        <p><span class="vote_count">{{voteCount}}</span></p>
        <p><span class="message-content">{{content}}</span></p><br>

      <div class="message_comments">
        {{#comments}}
          <span class="comment_content">{{comment.content}} |</span>
          <span class="comment_username">{{username}} |</span>
          <span class="comment_vote_count">{{comment_vote_count}}</span>
          <br>
        {{/comments}}
      </div>

      <div>
        <input type="text_area" placeholder="Drop a comment!" id="comment_input">
        <br>
        <input type="submit" value="Comment" id="comment_submit">
        <button id="comment_cancel">Cancel</button>
      </div>

      <footer>
        <%= button_to "Close Message", "", :id => "message-close", :class => 'btn btn-danger' %>
      </footer>
    </div>
  </script>

  <script type="text/javascript" src="//api.filepicker.io/v1/filepicker.js"></script>

</body>
</html>


